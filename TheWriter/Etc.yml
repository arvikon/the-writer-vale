---
extends: substitution
message: Use '%s' instead of '%s'
link: http://www.thewriter.com/what-we-think/style-guide/etc/
level: error
ignorecase: true
nonword: true
action:
  name: replace
  params:
    - and so on
    - like
swap:
  '\b(?:etc)\b\.?': and so on' or 'like
